#!/usr/bin/env sh

# Lint staged TS files via lint-staged.
npx lint-staged

# Rebuild dist/ if any TS source changed (so committed bundle stays fresh).
if git diff --cached --name-only | grep -qE '^src/.*\.ts$'; then
  echo "TS source changed — rebuilding dist/"
  npm run build
  git add dist/
fi
