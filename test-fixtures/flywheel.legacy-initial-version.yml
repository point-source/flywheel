# Legacy v1 key `initial_version` — both validators must report a warning
# (not an error) so the config still loads. Migration hint points at the
# git-tag baseline workflow documented in docs/adopter-setup.md §0.1.
flywheel:
  initial_version: 1.0.0
  streams:
    - name: main-line
      branches:
        - name: main
          auto_merge: [fix]
