flywheel:
  streams:
    - name: main-line
      branches:
        - { name: main, release: production, auto_merge: [] }
  release_files:
    - path: pubspec.yaml
      pattern: ''
      replacement: 'version: ${version}'
  merge_strategy: squash
